node_modules
.next
out
.DS_Store
*.log
tsconfig.tsbuildinfo

# Environment variables (Time Tracking app secrets: DATABASE_URL, AUTH_SECRET)
.env
.env.local
.env.*.local

# QA suite run artefacts (the reports are per-run, not source of truth)
qa/reports/
